<!DOCTYPE html>
<html>
<head>
    <title>EMG Data Monitor</title>
    <script>
        function fetchEmgData() {
            fetch('/emg')
                .then(response => response.json())
                .then(data => {
                    document.getElementById('emgValue').innerText = data.emg_value;
                })
                .catch(error => console.error('Error:', error));
        }

        setInterval(fetchEmgData, 1000); // 1초마다 데이터 갱신
    </script>
</head>
<body>
    <h1>EMG Data Monitor</h1>
    <p>Current EMG Value: <span id="emgValue">Loading...</span></p>
</body>
</html>
